# Alternative to config.json — use either, config.json takes precedence

GROUP_JID=120363XXXXXXXXX@g.us

# Comma-separated Hebrew city names (must match Pikud Ha'oref exactly)
CITIES=תל אביב - מרכז העיר,תל אביב - מזרח,רמת גן - מרכז

# Comma-separated category numbers: 1=missiles,2=aircraft,5=terrorists
ALERT_CATEGORIES=1,2,5

POLL_INTERVAL_MS=2000
AUTH_DIR=./auth
